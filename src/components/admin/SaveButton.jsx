const SaveButton = () => {
  return (
    <button
      type="submit"
      className="w-full bg-primary-blue text-white font-inter text-sm font-medium py-2 px-3  rounded"
    >
      Save
    </button>
  );
};

export default SaveButton;
